node_modules/
dist/
*.local
.DS_Store
.vite/

# Generated data (regenerate with `npm run seed`). The large dictionary in
# particular shouldn't live in git.
public/data/dict/
public/data/books/
private_books

# Per-book extraction profiles cover specific (often non-public-domain) books
# and live only on the machine that has the source; the loader treats them as
# optional. See scripts/profiles/index.mjs.
scripts/profiles/book/

.env